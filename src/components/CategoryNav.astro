---
const { active = '' } = Astro.props;
const categories = [
  { label: 'すべて', slug: '' },
  { label: 'お金・キャリア', slug: 'money-career' },
  { label: '地方移住・暮らし', slug: 'local-life' },
  { label: '子育て・共働き', slug: 'parenting' },
  { label: '食・卵・富山', slug: 'food' },
  { label: '健康・その他', slug: 'health' },
];
---

<nav class="cat-nav">
  {categories.map(c => (
    <a
      href={c.slug ? `/category/${c.slug}` : '/'}
      class={`cat-link ${active === c.slug ? 'active' : ''}`}
    >
      {c.label}
    </a>
  ))}
</nav>

<style>
.cat-nav {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
}

.cat-link {
  padding: 6px 16px;
  border: 1px solid var(--border);
  border-radius: 2px;
  font-size: 0.78rem;
  font-weight: 500;
  letter-spacing: 0.08em;
  color: var(--gray);
  transition: all 0.2s;
}

.cat-link:hover,
.cat-link.active {
  background: var(--black);
  border-color: var(--black);
  color: #fff;
}
</style>
